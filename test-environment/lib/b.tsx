import React from 'react';
import * as ReactDOMClient from 'react-dom/client'

import './style.sass';

const container = document.getElementById('root');
const root = ReactDOMClient.createRoot(container);

root.render(<div>AAAAAAAAAAAAA</div>);
